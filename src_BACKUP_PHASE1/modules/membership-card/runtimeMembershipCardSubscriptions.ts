import {
  runtimeLogger,
} from "@/runtime/logger/runtimeLogger";

export async function initializeMembershipCardSubscriptions() {

  runtimeLogger.info("APP", 
    "[CARD] Membership subscriptions ready"
  );

}
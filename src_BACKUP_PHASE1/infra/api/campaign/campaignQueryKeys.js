/**
 * =========================================================
 * CAMPAIGN QUERY ROOT
 * =========================================================
 */

const ROOT =
  "campaigns";

/**
 * =========================================================
 * CAMPAIGN QUERY KEYS
 * =========================================================
 */

export const CAMPAIGN_QUERY_KEYS =
  Object.freeze({

    /**
     * =====================================================
     * ROOT
     * =====================================================
     */

    ALL:
      [ROOT],

    /**
     * =====================================================
     * LIST
     * =====================================================
     */

    LIST:
      () => [
        ROOT,
        "list",
      ],

    /**
     * =====================================================
     * DETAIL
     * =====================================================
     */

    DETAIL:
      (campaignId) => [

        ROOT,
        "detail",
        String(campaignId),

      ],

    /**
     * =====================================================
     * ACTIVE
     * =====================================================
     */

    ACTIVE:
      () => [

        ROOT,
        "active",

      ],

    /**
     * =====================================================
     * FEATURED
     * =====================================================
     */

    FEATURED:
      () => [

        ROOT,
        "featured",

      ],

    /**
     * =====================================================
     * POPUP
     * =====================================================
     */

    POPUP:
      () => [

        ROOT,
        "popup",

      ],

    /**
     * =====================================================
     * BANNER
     * =====================================================
     */

    BANNER:
      () => [

        ROOT,
        "banner",

      ],

    /**
     * =====================================================
     * FLASH SALE
     * =====================================================
     */

    FLASH_SALE:
      () => [

        ROOT,
        "flash-sale",

      ],

    /**
     * =====================================================
     * LOYALTY
     * =====================================================
     */

    LOYALTY:
      () => [

        ROOT,
        "loyalty",

      ],

    /**
     * =====================================================
     * GAME
     * =====================================================
     */

    GAME:
      () => [

        ROOT,
        "game",

      ],

    /**
     * =====================================================
     * TYPE
     * =====================================================
     */

    TYPE:
      (type) => [

        ROOT,
        "type",
        String(type),

      ],

    /**
     * =====================================================
     * SEARCH
     * =====================================================
     */

    SEARCH:
      (keyword) => [

        ROOT,
        "search",
        String(keyword),

      ],

    /**
     * =====================================================
     * PAGINATION
     * =====================================================
     */

    PAGINATED:
      ({
        page = 1,
        limit = 20,
        type,
        featured,
        active,
      } = {}) => [

        ROOT,

        "paginated",

        {
          page,
          limit,
          type:
            type || null,

          featured:
            featured ?? null,

          active:
            active ?? null,
        },

      ],

    /**
     * =====================================================
     * ADMIN
     * =====================================================
     */

    ADMIN:
      () => [

        ROOT,
        "admin",

      ],

    /**
     * =====================================================
     * ANALYTICS
     * =====================================================
     */

    ANALYTICS:
      () => [

        ROOT,
        "analytics",

      ],

    /**
     * =====================================================
     * PERFORMANCE
     * =====================================================
     */

    PERFORMANCE:
      () => [

        ROOT,
        "performance",

      ],

    /**
     * =====================================================
     * WEBVIEW
     * =====================================================
     */

    WEBVIEW:
      () => [

        ROOT,
        "webview",

      ],

    /**
     * =====================================================
     * MOBILE
     * =====================================================
     */

    MOBILE:
      () => [

        ROOT,
        "mobile",

      ],

  });

export default
  CAMPAIGN_QUERY_KEYS;
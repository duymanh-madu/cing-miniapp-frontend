/**
 * =========================================================
 * LOYALTY QUERY ROOT
 * =========================================================
 */

const ROOT =
  "loyalty";

/**
 * =========================================================
 * LOYALTY QUERY KEYS
 * =========================================================
 */

export const LOYALTY_QUERY_KEYS =
  Object.freeze({

    /**
     * =====================================================
     * ROOT
     * =====================================================
     */

    ALL:
      [ROOT],

    /**
     * =====================================================
     * PROFILE
     * =====================================================
     */

    PROFILE:
      (userId) => [

        ROOT,
        "profile",
        String(userId),

      ],

    /**
     * =====================================================
     * MEMBERSHIP
     * =====================================================
     */

    MEMBERSHIP:
      (userId) => [

        ROOT,
        "membership",
        String(userId),

      ],

    /**
     * =====================================================
     * MEMBERSHIP DETAIL
     * =====================================================
     */

    MEMBERSHIP_DETAIL:
      (userId) => [

        ROOT,
        "membership-detail",
        String(userId),

      ],

    /**
     * =====================================================
     * MEMBERSHIP TYPES
     * =====================================================
     */

    MEMBERSHIP_TYPES:
      () => [

        ROOT,
        "membership-types",

      ],

    /**
     * =====================================================
     * TRANSACTIONS
     * =====================================================
     */

    TRANSACTIONS:
      ({
        userId,
        page = 1,
        limit = 20,
        type,
      } = {}) => [

        ROOT,

        "transactions",

        String(userId),

        {
          page,
          limit,
          type:
            type || null,
        },

      ],

    /**
     * =====================================================
     * TRANSACTION DETAIL
     * =====================================================
     */

    TRANSACTION_DETAIL:
      (transactionId) => [

        ROOT,
        "transaction-detail",
        String(transactionId),

      ],

    /**
     * =====================================================
     * LEADERBOARD
     * =====================================================
     */

    LEADERBOARD:
      ({
        period = "all-time",
        limit = 10,
      } = {}) => [

        ROOT,

        "leaderboard",

        {
          period,
          limit,
        },

      ],

    /**
     * =====================================================
     * REWARDS
     * =====================================================
     */

    REWARDS:
      (userId) => [

        ROOT,
        "rewards",
        String(userId),

      ],

    /**
     * =====================================================
     * MISSIONS
     * =====================================================
     */

    MISSIONS:
      (userId) => [

        ROOT,
        "missions",
        String(userId),

      ],

    /**
     * =====================================================
     * STREAKS
     * =====================================================
     */

    STREAKS:
      (userId) => [

        ROOT,
        "streaks",
        String(userId),

      ],

    /**
     * =====================================================
     * ANALYTICS
     * =====================================================
     */

    ANALYTICS:
      (userId) => [

        ROOT,
        "analytics",
        String(userId),

      ],

    /**
     * =====================================================
     * METRICS
     * =====================================================
     */

    METRICS:
      () => [

        ROOT,
        "metrics",

      ],

    /**
     * =====================================================
     * ADMIN
     * =====================================================
     */

    ADMIN:
      () => [

        ROOT,
        "admin",

      ],

    /**
     * =====================================================
     * REALTIME
     * =====================================================
     */

    REALTIME:
      (userId) => [

        ROOT,
        "realtime",
        String(userId),

      ],

  });

export default
  LOYALTY_QUERY_KEYS;
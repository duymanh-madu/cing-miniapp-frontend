/**
 * =========================================================
 * MENU QUERY ROOT
 * =========================================================
 */

const ROOT =
  "menu";

/**
 * =========================================================
 * MENU QUERY KEYS
 * =========================================================
 */

export const MENU_QUERY_KEYS =
  Object.freeze({

    /**
     * =====================================================
     * ROOT
     * =====================================================
     */

    ALL:
      [ROOT],

    /**
     * =====================================================
     * ITEMS
     * =====================================================
     */

    ITEMS:
      ({
        category,
        keyword,
        featured,
        active = true,
        available,
        page = 1,
        limit = 100,
      } = {}) => [

        ROOT,

        "items",

        {

          category:
            category || null,

          keyword:
            keyword || null,

          featured:
            featured ?? null,

          active,

          available:
            available ?? null,

          page,

          limit,

        },

      ],

    /**
     * =====================================================
     * ITEM DETAIL
     * =====================================================
     */

    ITEM:
      (id) => [

        ROOT,
        "item",
        String(id),

      ],

    /**
     * =====================================================
     * FEATURED
     * =====================================================
     */

    FEATURED:
      ({
        page = 1,
        limit = 20,
      } = {}) => [

        ROOT,

        "featured",

        {
          page,
          limit,
        },

      ],

    /**
     * =====================================================
     * CATEGORIES
     * =====================================================
     */

    CATEGORIES:
      () => [

        ROOT,
        "categories",

      ],

    /**
     * =====================================================
     * CATEGORY DETAIL
     * =====================================================
     */

    CATEGORY:
      (categoryId) => [

        ROOT,
        "category",
        String(categoryId),

      ],

    /**
     * =====================================================
     * CATEGORY ITEMS
     * =====================================================
     */

    CATEGORY_ITEMS:
      ({
        categoryId,
        page = 1,
        limit = 100,
      } = {}) => [

        ROOT,

        "category-items",

        String(categoryId),

        {
          page,
          limit,
        },

      ],

    /**
     * =====================================================
     * SEARCH
     * =====================================================
     */

    SEARCH:
      ({
        keyword,
        page = 1,
        limit = 50,
      } = {}) => [

        ROOT,

        "search",

        {
          keyword:
            keyword || "",

          page,

          limit,

        },

      ],

    /**
     * =====================================================
     * INVENTORY
     * =====================================================
     */

    INVENTORY:
      () => [

        ROOT,
        "inventory",

      ],

    /**
     * =====================================================
     * INVENTORY ITEM
     * =====================================================
     */

    INVENTORY_ITEM:
      (itemId) => [

        ROOT,
        "inventory-item",
        String(itemId),

      ],

    /**
     * =====================================================
     * REALTIME
     * =====================================================
     */

    REALTIME:
      () => [

        ROOT,
        "realtime",

      ],

    /**
     * =====================================================
     * IPOS SYNC
     * =====================================================
     */

    IPOS_SYNC:
      () => [

        ROOT,
        "ipos-sync",

      ],

    /**
     * =====================================================
     * ANALYTICS
     * =====================================================
     */

    ANALYTICS:
      () => [

        ROOT,
        "analytics",

      ],

  });

export default
  MENU_QUERY_KEYS;
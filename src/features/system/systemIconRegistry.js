import {
  Home,
  Coffee,
  Trophy,
  User,
  Gamepad2,
} from "lucide-react";

const systemIconRegistry = {
  home: Home,

  coffee: Coffee,

  trophy: Trophy,

  user: User,

  gamepad: Gamepad2,
};

export default systemIconRegistry;